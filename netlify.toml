[build]
  base    = "client"
  command = "npm install && npm run build"
  publish = "../dist/public"   # Go up one level, then into dist/public

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
